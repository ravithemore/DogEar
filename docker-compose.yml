version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: bookverse-db
    environment:
      POSTGRES_DB: bookverse
      POSTGRES_USER: bookverse_user
      POSTGRES_PASSWORD: bookverse_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bookverse_user -d bookverse"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: bookverse-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
